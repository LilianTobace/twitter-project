{
  "name": "brandbastion-project",
  "version": "1.0.0",
  "description": "Test for Brand Bastion company",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "start": "npx tsc && nodemon --exec ts-node ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -p tsconfig.build.json",
    "copy-resources": "copyfiles -u 1 src/*/.json dist/",
    "build": "npm run clean && npm run compile && npm run copy-resources"
  },
  "keywords": [],
  "author": "LilianTobace",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "copyfiles": "^2.4.1",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
